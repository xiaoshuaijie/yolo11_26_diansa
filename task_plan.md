# Task Plan: MaixPy 实机视觉阈值校准

## Goal
基于 MaixVision 的实时相机画面，消除黑色车架对钢珠识别的干扰，并将经过验证的 OpenCV 参数写回实际运行项目。

## Current Phase
Phase 3

## Phases

### Phase 1: 代码与设备发现
- [x] 定位 OpenCV 图像预处理、阈值、ROI 与巡线决策代码
- [x] 启动/连接 MaixVision 并确认实时画面来源
- [x] 记录黑色车架与钢珠在画面中的相对位置
- **Status:** complete

### Phase 2: 实机参数采样与方案确定
- [x] 在 MaixVision 中取得至少一帧可用真实画面
- [x] 观察模板回退中的车架误检区域
- [x] 确定以检测器确认、模板阈值和纹理门控为主的最小调整方案
- **Status:** complete

### Phase 3: 代码实施
- [x] 修改仅与 OpenCV 检测相关的参数或防护逻辑
- [x] 使调参值集中、可复现，并保持原有接口
- **Status:** pending

### Phase 4: 验证
- [ ] 对真实画面检查黑线保留、车架排除与转弯场景
- [ ] 运行可在本机执行的语法/单元验证
- **Status:** pending

### Phase 5: 交付
- [ ] 汇总改动、最终参数与现场复测步骤
- **Status:** pending

## Key Questions
1. 车架在相机画面中的固定遮挡区域具体在哪里？
2. 当前项目使用的是灰度阈值、HSV 阈值还是其他 OpenCV 分割方式？
3. MaixVision 是否已连接到目标 Maix 设备并能提供实时帧？

## Decisions Made
| Decision | Rationale |
|----------|-----------|
| 以实时 MaixVision 画面作为阈值依据 | 用户明确要求按真实硬件画面调节，照片仅作为赛道与车架背景信息 |
| 优先最小化修改 OpenCV 参数/ROI | 降低对现有控制与通信逻辑的回归风险 |
| 当前目标是钢珠检测而非巡线 | 实际运行项目与实时叠加层均为 `HybridBallTracker`，赛道照片只提供黑色背景风险信息 |
| 模板只能在近期 NPU 检测确认后短暂补帧 | 阻止错误模板在静态黑色车架上无限续期 |
| 标定轴使用 `(21, 80)` 到 `(460, 80)` | 用户截图中红色标记与原青色端点相差约 16/15 个原始像素，内收后两端分别对应 -12.5/+12.5cm |

## Errors Encountered
| Error | Attempt | Resolution |
|-------|---------|------------|
| 两个并行代码探索任务被服务限流 | 1 | 主任务直接完成代码和设备检查，未重复发起相同请求 |
| 默认 Python 3.14 缺少 `numpy`，无法运行跟踪器行为测试 | 1 | 改用项目已有 OpenCV/Numpy 的 Python 解释器，不重试相同环境 |
| PowerShell 的解释器探测条件写法无效 | 1 | 已定位到 Python 3.12，改为直接调用该解释器 |
| Python 3.12 缺少 `cv2`，无法本机加载真实跟踪器 | 2 | 不安装新依赖；用无依赖桩覆盖状态机，再以 MaixVision 设备端做真实 OpenCV 验证 |
| MaixVision“运行当前文件”只上传 `main.py`，缺少同目录模块 | 1 | 改用 MaixVision 项目运行入口，避免重复使用单文件运行 |
| 调试记录补丁因上下文已变化未能应用 | 1 | 已读取当前文件并使用精确上下文补写记录 |
