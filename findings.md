# Findings & Decisions

## Requirements
- 仔细阅读 `E:\jie\maixpy-skill`。
- 通过 `MaixVision.lnk` 连接/读取真实设备画面，调节 OpenCV 部分的阈值。
- 赛道为白底黑线；车架为黑色，须避免被误识别为赛道线。

## Research Findings
- 用户提供的赛道照片显示：白色底布上的宽黑色闭环/弯道线，周围也存在胶带标记；这说明黑色背景物会干扰视觉任务，但当前实际代码不是巡线算法。
- 用户提供的车架照片显示：车架主体与胶带均为深黑色；若进入摄像头下方或边缘视野，需依赖 ROI/几何约束而不只是放宽灰度阈值。
- MaixVision 已连接到目标设备，设备输出为 `480x160`；当前运行的是根目录的 `H_DEMO_HYBRID` 项目，而不是仅静态查看照片。
- 连续两帧实时画面中没有可见钢珠，但叠加层持续显示 `BALL -10.93cm T`，其中 `T` 是 OpenCV 模板跟踪来源；黄色检测框落在车架/安装结构的静态区域。这是模板阈值过宽或错误模板被保留导致的实机误检。
- `E:\jie` 与 MaixVision 打开的 `C:\Users\24137\Downloads\H_demo_hybrid\H_demo_hybrid` 是不同目录；三份运行文件在调整前哈希相同，因此本轮把检测器改动同步到两处，保证 IDE 复测的是更新后的代码。
- 用户标出的白色导轨内侧端点对应当前 `480px` 画面的约 `x=21` 与 `x=460`；原来 `(5,80)` 到 `(475,80)` 的青色十字落在画面边缘之外的安装区域。

## Technical Decisions
| Decision | Rationale |
|----------|-----------|
| 实机观察后再定数值 | 摄像头曝光、安装角度和环境光会让静态照片阈值不可靠 |
| 优先排除固定车架遮挡区 | 黑色车架和黑线颜色相同，颜色阈值无法可靠区分 |
| 收紧 OpenCV 模板跟踪并增加有效性门控 | 实机已经证明当前 `T` 路径会持续把静态车架当作钢珠 |
| 将模板门限调整为 `T=0.70`、`G=0.78` | 当前的 `0.48/0.60` 允许车架纹理形成稳定匹配，先提高为保守初值再实机观察 |
| `T/G` 仅在最近 320ms 有 Y/E 确认时有效 | 黑色静态车架不能通过模板本身无限更新 `last_seen_ms` |
| 标定范围仍为 -12.5cm 到 +12.5cm | 仅将像素端点改到导轨实际端点，物理刻度不变 |

## Issues Encountered
| Issue | Resolution |
|-------|------------|
|       |            |

## Resources
- `E:\jie\maixpy-skill`
- `C:\Users\24137\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\MaixVision.lnk`
- 用户提供的三张实物照片

## Visual/Browser Findings
- 赛道：白底、黑色粗线，含直线与 U 形弯道；边缘黑胶带标记不可被当成巡线主目标。
- 车架：深黑色结构，预计在镜头固定安装位置形成稳定的深色遮挡/反射区域。
- MaixVision 实时图：画面上方/中部包含固定黑色车架与浅色横梁；当前检测框和 `T` 标记在没有钢珠时仍稳定存在，说明需优先修复跟踪回退逻辑而非单纯降低 YOLO 阈值。
- 用户提供的标定截图：青色端点横向位于原始画面的约 `x=5/475`，红色物理端点位于约 `x=21/460`；两端的物理值应分别固定为 `-12.5/+12.5cm`。
